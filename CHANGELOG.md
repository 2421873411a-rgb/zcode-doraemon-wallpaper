# 版本变更记录（CHANGELOG）

> 每次迭代前都会备份 `app.asar` 到 `versions/`，回滚方法见末尾。

---

## v1.0 · 四时壁纸 + 全透明 + 无框按钮（已上线）
**时间**：2026-07-27
**快照**：`versions/app.asar.v1.0-四时壁纸无框版`

### 功能
- 四时段壁纸自动切换（哆啦A梦主题）
  - 🌅 清晨 05:00-08:00 / ☀️ 白天 08:00-17:00 / 🌆 黄昏 17:00-19:00 / 🌙 夜晚 19:00-05:00
- 全透明 UI（所有容器/面板背景透明，壁纸完全透出）
- 无框按钮（仅悬停浮现反馈）
- 时段配色：清晨/白天黑字+白描边，黄昏/夜晚白字+黑描边
- 切换为 1.2s 淡入淡出，每分钟校准，休眠唤醒重校准

### 文件结构
```
inject/
├── css.css      ← 样式（壁纸层/透明UI/配色/无框按钮）
├── body.html    ← 壁纸层 HTML
└── script.js    ← 四时段切换逻辑
wallpapers/
├── doraemon-morning.png / day.png / dusk.png / night.png
```

---

## v2.0 · 雨天壁纸矩阵 + 手动开关（已上线）
**时间**：2026-07-27
**状态**：✅ 已完成并测试通过
**快照**：`versions/app.asar.v2.0-雨天矩阵手动开关`

### 新增功能
- **双套壁纸矩阵**：晴（clear/）和雨（rain/）各 4 张，共 8 张
- **天气维度选择**：壁纸路径变为 `wallpapers/{weather}/doraemon-{period}.png`
- **手动切换晴/雨**，三种方式：
  - 快捷键 **Ctrl+Shift+R** 切换
  - 右下角隐形小按钮（🌤️/🌧️，悬停浮现，点击切换）
  - 控制台输入 `__dwWeather('rain')` / `'clear'` / `'toggle'`
- **状态持久化**：选择存在 localStorage（`dw-weather`），重启 ZCode 后保留
- **切换反馈**：右下角弹出 1.5 秒提示气泡（"壁纸已切换：🌧️ 雨天"）
- **v1.0 升级支持**：apply.js 自动识别并清理 v1.0 的无标记手工注入，平滑升级
- **为 v3.0 预留**：`weather-config.json` 含坐标、时间表、降水阈值（v3.0 启用 open-meteo 自动检测时用）

### 文件结构（v2.0）
```
inject/
├── css.css         ← 样式（不变）
├── body.html       ← 壁纸层 HTML（不变）
├── script.js       ← 二维选择（时段 × 天气）+ 暴露 __dwSetWeather 等接口
└── switcher.js     ← 手动开关（快捷键 + 按钮 + 提示）  [新增]
wallpapers/
├── clear/          ← 晴天 4 张  [重组]
└── rain/           ← 雨天 4 张  [新增]
weather-config.json ← 天气配置（坐标/时间表/阈值）  [新增]
```

### 用法
- 默认晴天。下雨时按 **Ctrl+Shift+R** 切到雨天壁纸
- 选择会记住，下次打开 ZCode 还是上次的状态

---

## v3.0 · open-meteo 自动天气检测（已上线）
**时间**：2026-07-28
**状态**：✅ 已完成并测试通过
**快照**：`versions/app.asar.v3.0-open-meteo自动检测`

### 新增功能
- **open-meteo 自动检测下雨**：免费、免 key、支持 CORS（浏览器直接 fetch）
- **按时间表自动切换坐标**：
	- 周一~周五 08:00-17:00 → 工作地（39.9142, 116.4174）
	- 其余时间 → 家（39.9042, 116.4074）
- **滞回阈值判断**：进入雨境需 precip≥0.1mm/h 或概率≥50%；退出需 precip≤0.05 且概率≤30%。防止临界抖动
- **每 20 分钟刷新**一次天气，休眠唤醒后立即重测
- **降级机制**：fetch 失败/超时（8秒）→ 沿用上次结果，不切换
- **手动优先**：用户手动切换（Ctrl+Shift+R/按钮）会锁定，自动检测让位；控制台 `__dwWeather('auto')` 恢复自动
- **状态模型升级**：`dw-weather-source` 区分 manual/auto，解决"自动检测只生效一次"的 bug

### apply.js 重大改进
- **去掉 npx 依赖**：改用本地 asar@3.2.0（CommonJS），重装零网络依赖
- **修复 asarPack async bug**：createPackage 是 async，之前没 await 导致错误静默丢失
- **真实环境往返验证**：extract→inject→pack 闭环测试通过，index.html 零损失

### 文件结构（v3.0）
```
inject/
├── css.css, body.html, script.js, switcher.js  ← 不变
└── weather.js     ← open-meteo 自动检测（滞回+降级+坐标时间表）  [新增]
```

### 用法
- 默认开启自动检测：打开 ZCode 后 5 秒开始，自动判断晴/雨
- 手动锁定：Ctrl+Shift+R（自动检测暂停）
- 恢复自动：控制台 `__dwWeather('auto')`
- 查看状态：控制台 `__dwWeather()` → `{weather, source}`

---

## v3.2 · 双向描边（已上线）
**时间**：2026-07-28
**状态**：✅ 已完成
**快照**：`versions/app.asar.v3.2-双向描边`

### 改动
解决「亮壁纸局部暗区黑字看不清」问题。从单向阴影改为双向描边：
- **白天档（黑字）**：四方向白描边 `rgba(255,255,255,0.92)` + 轻黑投影。亮区是干净黑字，暗区靠白描边托起。
- **夜晚档（白字）**：四方向黑描边 `rgba(0,0,0,0.9)` + 柔和投影。对称处理。
- 任意亮度区域都有对比度。

> 只改 `inject/css.css` 的 `--dw-shadow` 变量。

---

## v3.1 · 字体对比度优化（已上线）
**时间**：2026-07-28
**状态**：✅ 已完成
**快照**：`versions/app.asar.v3.1-字体对比度优化`

### 改动
之前文字带过重的描边（三层阴影），导致黑字被白边稀释发灰、对比度反而下降。本次精简：
- **白天档（清晨/白天）**：文字加深到纯黑 `#000`，阴影从三层白描边改为**单层极淡光晕** `0 0 3px rgba(255,255,255,0.45)`。亮区几乎无感，只在壁纸深色区域兜底。
- **夜晚档（黄昏/夜晚）**：白字阴影从三层黑描边改为**单层柔和阴影** `0 1px 3px rgba(0,0,0,0.55)`，文字更清晰不发糊。

> 只改了 `inject/css.css` 的 `--dw-shadow` 变量，功能逻辑无变化。

---

## final · 外部主题架构（已上线）
**时间**：2026-07-28
**状态**：✅ 已完成
**快照**：`versions/app.asar.final-外部主题架构`

### 重大架构变更
- **主题移到 asar 外部**：`D:\应用\Zcode\resources\themes\`，**加主题不用重打包**
- 引擎从外部目录通过 `file://` 协议加载主题，同时含兜底清单（外部不可用时降级）
- 这是**最后一次重打包**，以后所有主题操作都不需要等了

### 新增工具
- **`添加主题.bat`** / `node add-theme.js`：交互式添加新主题，自动复制文件 + 注册，然后重启 ZCode 即生效

### 用法
```
双击 添加主题.bat → 输入主题名 → 选类型(video/image) → 选素材文件 → 完成重启
或命令行：node add-theme.js video "主题名" "视频路径"
         node add-theme.js image "主题名" "图片路径"
```
**时间**：2026-07-28
**状态**：✅ 已完成
**快照**：`versions/app.asar.v4.0-多主题视频壁纸`

### 新增功能
- **多主题架构**：壁纸系统从「单主题」升级为「主题平台」，可无限添加主题
  - 每个主题用 `theme.json` 声明：类型(静态/视频)、是否分时段/天气、配色、字体
  - 主题放 asar 内 `themes/`，换主题只改 localStorage（不重打包），加主题跑一次重装
- **主题选择界面**：快捷键 **Ctrl+Shift+W** 调出半透明面板，点击切换
- **视频壁纸支持**：主题可用 `<video>` 循环播放（首个示例：懒羊羊睡觉）
- **内置 2 个主题**：
  - `doraemon` 哆啦A梦四时（静态 + 时段 + 晴雨 + 自动检测）
  - `lazy-sheep` 懒羊羊睡觉（视频 + 不分时段天气）

### 架构
```
themes/
├── _registry.json       ← 主题清单
├── doraemon/            ← 主题1（静态四时）
│   ├── theme.json
│   └── clear/, rain/
└── lazy-sheep/          ← 主题2（视频）
    ├── theme.json
    └── bg.mp4
```
注入脚本增至 7 块：css/body/themeEngine/script/switcher/weather/themePanel

### 用法
- **Ctrl+Shift+W**：调出主题选择面板
- 控制台 `__dwListThemes()` 列主题、`__dwSwitchTheme('lazy-sheep')` 切换

---

## v5.0 · 后续展望（待开发）
**状态**：📋 候选

- 雨滴动画叠加层（Canvas 粒子，按降水强度调密度）
- 多天气扩展（雪/雾），不止晴雨两态
- 主题市集：从文件夹直接导入新主题（不重打包）
- 视频壁纸的音量/播放控制

---



## 回滚方法

### 回到某个版本
1. 进入 ZCode 安装目录 `D:\应用\Zcode\resources\`
2. 把 `versions/app.asar.vX.X-名称` 复制过去，改名为 `app.asar` 覆盖
3. 重启 ZCode

### 回到原始官方版
`resources/` 里有 `app.asar.bak.20260727_151209`（约 217MB，是最初的官方版），改名覆盖即可。

---

*每一步都可独立回滚。版本号只增不减，已上线的不覆盖。*
