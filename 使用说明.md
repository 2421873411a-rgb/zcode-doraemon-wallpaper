# ZCode 哆啦A梦四时壁纸 · 一键重装工具包

> 给 ZCode 加上「四时段壁纸自动切换 + 全透明 UI + 无框按钮」的机制。
> ZCode 每次升级覆盖 `app.asar` 后，壁纸机制会丢失 —— 双击本工具即可秒恢复。

---

## 🚀 怎么用

### 日常重装（ZCode 升级后）
1. **完全退出 ZCode**（任务管理器结束所有 `ZCode.exe`，或正常关闭）
2. 双击 **`一键重装.bat`**
3. 等待 1-3 分钟，看到 ✅ 完成提示
4. 重新打开 ZCode，壁纸机制就回来了

> 不退出 ZCode 也能跑，但替换 `app.asar` 时若被占用会报错，按提示退出重试即可。

### 查看当前状态（排查问题时用）
双击 **`查看状态.bat`**，会显示：
- 当前线上 app.asar 是哪个版本（v1/v2/v3）
- 5 个注入块是否齐全
- 壁纸数量是否正确
- 有哪些回溯快照和备份可用

只读检查，不改任何东西，随时可运行。

### 命令行运行
```bash
cd "C:\Users\86178\Desktop\ZCode壁纸工具包"
node apply.js     # 重装
node status.js    # 查看状态
```

---

## 📁 工具包内容

```
ZCode壁纸工具包\
├── 一键重装.bat          ← 双击：重装/升级壁纸机制
├── 查看状态.bat          ← 双击：检查当前版本和完整性（只读，安全）
├── apply.js              ← 重装核心脚本（解包/注入/重打包/替换+校验）
├── status.js             ← 状态自检脚本
├── 使用说明.md           ← 本文件
├── CHANGELOG.md          ← 版本变更记录（可回溯每一步）
├── versions\             ← 每个版本的 app.asar 快照（回滚用）
├── weather-config.json   ← 天气配置（坐标/时间表/阈值，v3.0自动检测用）
├── inject\               ← 注入模板（可自行修改）
│   ├── css.css           ← 壁纸层 + 透明UI + 时段配色 + 无框按钮样式
│   ├── body.html         ← 壁纸层 HTML 结构
│   ├── script.js         ← 四时段×天气 双维切换逻辑
│   └── switcher.js       ← 手动开关（快捷键+按钮+提示）
└── wallpapers\           ← 双套壁纸矩阵
    ├── clear\            ← 晴天 4 张
    │   ├── doraemon-morning.png   🌅 清晨 05:00-08:00
    │   ├── doraemon-day.png       ☀️ 白天 08:00-17:00
    │   ├── doraemon-dusk.png      🌆 黄昏 17:00-19:00
    │   └── doraemon-night.png     🌙 夜晚 19:00-05:00
    └── rain\             ← 雨天 4 张（同名）
```

---

## ⏰ 时段 × 天气 双维选择

**时段**（按本地时间自动切换，1.2 秒淡入淡出）：

| 时段 | 时间 | 文字颜色 |
|------|------|---------|
| 🌅 清晨 | 05:00 - 08:00 | **黑字**（亮壁纸配深字） |
| ☀️ 白天 | 08:00 - 17:00 | **黑字** |
| 🌆 黄昏 | 17:00 - 19:00 | **白字**（暗壁纸配浅字） |
| 🌙 夜晚 | 19:00 - 05:00 | **白字** |

**天气**（默认晴天，手动切换雨天）：

| 天气 | 壁纸目录 | 怎么切换 |
|------|---------|---------|
| ☀️ 晴 | `wallpapers/clear/` | 默认 |
| 🌧️ 雨 | `wallpapers/rain/` | 见下方"🌧️ 切换雨天壁纸" |

> 当前时段 × 当前天气，从对应目录取同名壁纸。例如白天+雨天 → `wallpapers/rain/doraemon-day.png`。

---

## 🌧️ 切换雨天壁纸（v2.0 新增）

下雨时，用下面任一方式切到雨天壁纸：

1. **快捷键 `Ctrl + Shift + R`** —— 最快，一键晴⇄雨
2. **右下角小按钮** —— 一个半透明的 🌤️/🌧️ 图标，鼠标移过去会浮现，点击切换
3. **控制台**（F12）—— 输入 `__dwWeather('rain')` 切雨、`'clear'` 切晴、`'toggle'` 切换

切换时会弹出 1.5 秒提示："壁纸已切换：🌧️ 雨天"。

**选择会记住**：存在浏览器本地存储里，下次打开 ZCode 还是上次的状态。

---

## 🌦️ 自动检测下雨（v3.0 新增）

打开 ZCode 后会**自动联网检测下雨**，下雨时自动切到雨天壁纸，停了再切回晴天。

**怎么工作的**：
- 数据源 [open-meteo.com](https://open-meteo.com)（免费、免 key，只传坐标不传任何身份）
- 按你的作息自动选坐标：
	- 周一~周五 08:00-17:00 → 工作地
	- 其余时间 → 家
- 每 20 分钟查一次，**滞回判断**避免毛毛雨让壁纸反复横跳
- 下雨判断：降水量 ≥0.1mm/h **或** 降水概率 ≥50%
- 断网/API 失败 → 沿用上次结果，不会乱切

**手动 vs 自动**：
- 你按 Ctrl+Shift+R 手动切一次 → **自动检测暂停**（你说了算）
- 想恢复自动：按 F12 打开控制台，输入 `__dwWeather('auto')` 回车
- 查看当前状态：控制台输入 `__dwWeather()`，会显示 `{weather: 'clear'/'rain', source: 'manual'/'auto'}`

> 想改坐标/时间表/下雨阈值：编辑 `inject/weather.js` 顶部的 `CONFIG`，再重装。

---

## 🎨 多主题平台（v4.0 新增）

壁纸系统支持多个主题，每个主题可独立配置壁纸类型、时段/天气、配色。内置 2 个：

| 主题 | 类型 | 说明 |
|------|------|------|
| 哆啦A梦四时 | 静态 | 四时段 + 晴雨 + 自动检测（默认） |
| 懒羊羊睡觉 | 视频 | 循环播放的 mp4，不分时段天气 |

### 切换主题
- **快捷键 `Ctrl + Shift + W`**：调出主题选择面板，点击切换
- 控制台：`__dwListThemes()` 列出所有主题；`__dwSwitchTheme('lazy-sheep')` 切换

### 添加新主题
1. 在工具包 `themes/` 下建新文件夹（如 `my-theme/`）
2. 写 `theme.json` 声明配置：
   ```json
   {
     "id": "my-theme",
     "name": "我的主题",
     "type": "static",        // static 或 video
     "periods": false,         // 是否分时段
     "weather": false,         // 是否分晴雨
     "asset": "bg.png",        // video 类型用；static 用下面的 assets
     "desc": "我的自定义主题"
   }
   ```
3. 放入壁纸素材（图片或视频）
4. 在 `themes/_registry.json` 的 `themes` 数组加入主题 id
5. 双击 `一键重装.bat` 重新打包

> 加主题需要重打包 asar（约 5-12 分钟），但**切换主题不用**——切换只改本地存储，瞬间生效。

---

## 🎨 自定义

### 换壁纸
- **晴天壁纸**：放进 `wallpapers/clear/`，文件名保持 `doraemon-morning.png` 等
- **雨天壁纸**：放进 `wallpapers/rain/`，同名
- 改完双击 `一键重装.bat` 重新应用即可
> 想换文件名/增减时段：同时改 `inject/script.js` 里的 `PERIODS` 数组。

### 调配色（文字颜色、按钮样式）
改 `inject/css.css`：
- 亮色档文字色：搜 `--dw-text: #111827`（清晨/白天）
- 暗色档文字色：搜 `--dw-text: #ffffff`（黄昏/夜晚，在默认 `:root` 段）
- 主按钮颜色：搜 `rgba(74, 144, 226, 0.7)`

### 调时段边界
改 `inject/script.js` 里 `currentPeriod()` 函数的 `if` 条件。

> **改完模板后，重新双击 `一键重装.bat` 即可生效。** 脚本是幂等的，会自动替换旧注入块。

---

## 🔧 工作原理（脚本做了什么）

1. 自动定位 ZCode 安装目录（先查 `ZCODE_DIR` 环境变量，再查常见路径和 PATH）
2. 把 `resources/app.asar` 解包到临时目录
3. **幂等注入** `out/renderer/index.html`：用标记注释包裹注入内容，重复运行自动替换旧块
4. 复制 4 张壁纸到 `out/renderer/wallpapers/`
5. 重新打包成新 `app.asar`
6. 备份原文件（带时间戳 `app.asar.bak.YYYYMMDDHHMMSS`）→ 替换

**完全不修改 ZCode 原有代码逻辑**，只追加一个独立的壁纸 + 主题层。

---

## ↩️ 回滚 / 卸载

### 临时回滚（恢复某次备份）
进入 ZCode 安装目录的 `resources\`，把 `app.asar.bak.*` 文件之一改名覆盖 `app.asar`。

### 彻底卸载壁纸机制
找到**最早的**那个备份（体积最小、约 217MB 的那个，是官方原版），改名覆盖 `app.asar`。

> 备份文件命名规则：`app.asar.bak.20260727153624`（年月日时分秒）。

---

## ❓ 故障排查

| 问题 | 解决 |
|------|------|
| 找不到 ZCode 安装目录 | 命令行执行：`set ZCODE_DIR=你的ZCode路径` 再 `node apply.js` |
| 替换失败：文件被占用 | 完全退出 ZCode（任务管理器查杀所有 ZCode.exe）后重跑 |
| 双击 bat 一闪而过 | 说明没装 Node.js，去 https://nodejs.org 安装 |
| 注入后白屏 | 可能新版 index.html 结构大改，把最早的备份恢复，联系我适配 |
| 字体看不清 | 改 `inject/css.css` 的 `--dw-shadow` 描边强度后重装 |

---

## 📌 环境要求

- **Node.js**（任意较新版本，用于跑脚本和 asar 工具，首次运行会自动下载 asar 包）
- Windows 系统（脚本用了 `tasklist` 检测进程；macOS/Linux 需小改）

---

*Made with 💙 for ZCode · 哆啦A梦主题*
